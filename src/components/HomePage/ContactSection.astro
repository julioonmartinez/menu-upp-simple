<section class="contact-section" id="contacto">
    <div class="container">
      <div class="contact-content">
        <div class="contact-info animate-on-scroll">
          <div class="section-badge">
            <i class="fas fa-rocket"></i>
            <span>¡Tu Momento es Ahora!</span>
          </div>
          
          <h2 class="contact-title">Transforma tu Restaurante con <span class="text-orange">MenuUpp</span></h2>
          
          <p class="contact-description">
            Únete a la revolución digital gastronómica. Creamos experiencias de menú 
            exclusivas que potencian tu marca y encantan a tus clientes.
          </p>
          
          <div class="contact-features">
            <div class="feature-item">
              <i class="fas fa-check-circle"></i>
              <span>Diseño 100% personalizado</span>
            </div>
            <div class="feature-item">
              <i class="fas fa-check-circle"></i>
              <span>Analytics avanzados incluidos</span>
            </div>
            <div class="feature-item">
              <i class="fas fa-check-circle"></i>
              <span>Soporte dedicado 24/7</span>
            </div>
            <div class="feature-item">
              <i class="fas fa-check-circle"></i>
              <span>Implementación en 48 horas</span>
            </div>
          </div>
          
          <div class="contact-methods">
            <a href="mailto:hola@menuupp.com" class="contact-btn primary">
              <i class="fas fa-envelope"></i>
              <span>hola@menuupp.com</span>
            </a>
            <a href="https://wa.me/5551234567" class="contact-btn secondary">
              <i class="fab fa-whatsapp"></i>
              <span>WhatsApp</span>
            </a>
          </div>
        </div>
        
        <div class="contact-form animate-on-scroll">
          <form class="form">
            <div class="form-header">
              <h3>Solicita tu Demostración</h3>
              <p>Te contactamos en menos de 24 horas</p>
            </div>
            
            <div class="form-group">
              <label for="name">Nombre del Restaurante</label>
              <input type="text" id="name" name="name" required />
            </div>
            
            <div class="form-group">
              <label for="email">Email de Contacto</label>
              <input type="email" id="email" name="email" required />
            </div>
            
            <div class="form-group">
              <label for="phone">Teléfono</label>
              <input type="tel" id="phone" name="phone" required />
            </div>
            
            <div class="form-group">
              <label for="message">Cuéntanos sobre tu restaurante</label>
              <textarea id="message" name="message" rows="4" 
                placeholder="Tipo de cocina, ubicación, objetivos..."></textarea>
            </div>
            
            <button type="submit" class="submit-btn">
              <span>Solicitar Demostración</span>
              <i class="fas fa-paper-plane"></i>
            </button>
          </form>
        </div>
      </div>
    </div>
  </section>
  <style>

  .contact-section {
    padding: var(--space-4xl) 0;
    background: var(--neutral-light);
  }
   .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-lg);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }
  
  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: start;
  }
  
  .contact-title {
    font-family: var(--font-primary);
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 800;
    line-height: 1.2;
    color: var(--primary-blue);
    margin-bottom: var(--space-lg);
  }
  
  .contact-description {
    font-size: 1.2rem;
    color: var(--secondary-gray);
    line-height: 1.6;
    margin-bottom: var(--space-2xl);
  }
  
  .contact-features {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    margin-bottom: var(--space-2xl);
  }
  
  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    font-weight: 500;
    color: var(--primary-blue);
  }
  
  .feature-item i {
    color: var(--primary-orange);
    font-size: 1.2rem;
  }
  
  .contact-methods {
    display: flex;
    gap: var(--space-lg);
    flex-wrap: wrap;
  }
  
  .contact-btn {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-lg) var(--space-xl);
    border-radius: var(--radius-full);
    font-weight: 600;
    text-decoration: none;
    transition: all var(--transition-normal);
  }
  
  .contact-btn.primary {
    background: var(--gradient-primary);
    color: var(--pure-white);
  }

  
  .contact-btn.secondary {
    background: #25D366;
    color: var(--pure-white);
  }
  
  .contact-btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  /* Form Styles */
  .form {
    background: var(--pure-white);
    padding: var(--space-2xl);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
  }
  
  .form-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }
  
  .form-header h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary-blue);
    margin-bottom: var(--space-sm);
  }
  
  .form-header p {
    color: var(--secondary-gray);
  }
  
  .form-group {
    margin-bottom: var(--space-xl);
  }
  
  .form-group label {
    display: block;
    font-weight: 600;
    color: var(--primary-blue);
    margin-bottom: var(--space-sm);
  }
  
  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: var(--space-lg);
    border: 2px solid var(--neutral-light);
    border-radius: var(--radius-md);
    font-size: 1rem;
    transition: border-color var(--transition-normal);
    background: var(--pure-white);
  }
  
  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-orange);
  }
  
  .submit-btn {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-lg);
    background: var(--gradient-primary);
    color: var(--pure-white);
    border: none;
    border-radius: var(--radius-md);
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all var(--transition-normal);
  }
  
  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  @media (max-width: 768px) {
    .container {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
      text-align: center;
    }
    .contact-methods {
      flex-direction: column;
    }
    
    .contact-btn {
      justify-content: center;
      width: 100%;
    }
}

@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
    
    .floating-element,
    .phone-mockup,
    .cta-button {
      animation: none !important;
    }
  }
  
  /* High contrast mode */
  @media (prefers-contrast: high) {
    .btn-secondary {
      border: 2px solid currentColor;
    }
    
    .card-overlay {
      background: rgba(0, 0, 0, 0.8);
    }
  }
  
  /* Print styles */
  @media print {
    .floating-cta,
    .floating-elements,
    .hero-background {
      display: none !important;
    }
    
    .hero-section {
      background: white !important;
      color: black !important;
    }
  }
  </style>