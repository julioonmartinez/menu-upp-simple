  <section class="hero-section">
    <div class="hero-container">
      <!-- Background Video/Image -->
      <div class="hero-background">
        <div class="hero-gradient"></div>
      </div>
      
      <!-- Hero Content -->
      <div class="hero-content">
        <div class="hero-text">
          <h1 class="hero-title">
            <span class="title-main">Descubre, Valora y Comparte</span>
            <span class="title-highlight">tus Restaurantes Favoritos</span>
          </h1>
          <p class="hero-subtitle">
            Únete a la comunidad gastronómica más activa. 
            Encuentra nuevos sabores, comparte experiencias y conecta con otros food lovers.
          </p>
        </div>
        
        <!-- Search Bar -->
        <div class="hero-search">
          <div class="search-container">
            <div class="search-input-wrapper">
              <svg class="search-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              <input 
                type="text" 
                placeholder="Busca restaurantes, platillos o zonas..." 
                class="search-input"
                id="hero-search"
              />
              <button class="search-btn">
                Encontrar ahora
              </button>
            </div>
          </div>
        </div>
        
        <!-- CTAs -->
        <div class="hero-ctas">
          <button class="cta-primary">
            <svg class="cta-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
            Únete gratis
          </button>
          <button class="cta-secondary">
            <svg class="cta-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            Explorar sin cuenta
          </button>
        </div>
        
        <!-- Live Stats -->
        <div class="hero-stats">
          <div class="stat-item">
            <span class="stat-number" data-target="1247">0</span>
            <span class="stat-label">Valoraciones este mes</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-number" data-target="89">0</span>
            <span class="stat-label">Restaurantes activos</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-number" data-target="2156">0</span>
            <span class="stat-label">Food lovers</span>
          </div>
        </div>
      </div>
      
      <!-- Floating Elements -->
      <div class="floating-elements">
        <div class="floating-card card-1">
          <div class="card-content">
            <div class="card-avatar">🍕</div>
            <div class="card-info">
              <div class="card-name">Pizza Mario</div>
              <div class="card-rating">
                <span class="stars">⭐⭐⭐⭐⭐</span>
                <span class="rating-text">5.0</span>
              </div>
            </div>
          </div>
        </div>
        
        <div class="floating-card card-2">
          <div class="card-content">
            <div class="card-avatar">🌮</div>
            <div class="card-info">
              <div class="card-name">Tacos El Rey</div>
              <div class="card-rating">
                <span class="stars">⭐⭐⭐⭐</span>
                <span class="rating-text">4.8</span>
              </div>
            </div>
          </div>
        </div>
        
        <div class="floating-card card-3">
          <div class="card-content">
            <div class="card-avatar">🍜</div>
            <div class="card-info">
              <div class="card-name">Ramen Zen</div>
              <div class="card-rating">
                <span class="stars">⭐⭐⭐⭐⭐</span>
                <span class="rating-text">4.9</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <style>
    /* Hero Section */
    .hero-section {
      min-height: 100vh;
      display: flex;
      align-items: center;
      background: linear-gradient(135deg, var(--secondary-color) 0%, var(--secondary-light) 100%);
      position: relative;
      overflow: hidden;
    }
    
    .hero-background {
      position: absolute;
      inset: 0;
      z-index: 1;
    }
    
    .hero-gradient {
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse at center, rgba(255, 107, 0, 0.1) 0%, transparent 70%);
    }
    
    .hero-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem 1rem;
      position: relative;
      z-index: 2;
      width: 100%;
    }
    
    .hero-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    
    .hero-text {
      margin-bottom: 3rem;
      max-width: 800px;
    }
    
    .hero-title {
      margin-bottom: 1.5rem;
      line-height: 1.1;
    }
    
    .title-main {
      display: block;
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 700;
      color: #FFFFFF;
      margin-bottom: 0.5rem;
    }
    
    .title-highlight {
      display: block;
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 700;
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .hero-subtitle {
      font-size: clamp(1rem, 3vw, 1.25rem);
      color: var(--text-light);
      line-height: 1.6;
      opacity: 0.9;
    }
    
    /* Search */
    .hero-search {
      width: 100%;
      max-width: 600px;
      margin-bottom: 3rem;
    }
    
    .search-input-wrapper {
      display: flex;
      align-items: center;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 50px;
      padding: 0.75rem;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }
    
    .search-input-wrapper:focus-within {
      transform: translateY(-2px);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
    }
    
    .search-icon {
      width: 20px;
      height: 20px;
      color: var(--secondary-light);
      margin-left: 1rem;
      flex-shrink: 0;
    }
    
    .search-input {
      flex: 1;
      padding: 0.75rem 1rem;
      border: none;
      background: transparent;
      font-size: 1rem;
      color: var(--text-primary);
      outline: none;
    }
    
    .search-input::placeholder {
      color: var(--text-secondary);
      opacity: 0.7;
    }
    
    .search-btn {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
      color: white;
      border: none;
      padding: 0.875rem 2rem;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
      white-space: nowrap;
    }
    
    .search-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 20px rgba(255, 107, 0, 0.3);
    }
    
    /* CTAs */
    .hero-ctas {
      display: flex;
      gap: 1rem;
      margin-bottom: 3rem;
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .cta-primary,
    .cta-secondary {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }
    
    .cta-primary {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
      color: white;
    }
    
    .cta-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 30px rgba(255, 107, 0, 0.4);
    }
    
    .cta-secondary {
      background: transparent;
      color: #FFFFFF;
      border-color: rgba(255, 255, 255, 0.3);
      backdrop-filter: blur(10px);
    }
    
    .cta-secondary:hover {
      background: rgba(255, 255, 255, 0.1);
      border-color: rgba(255, 255, 255, 0.5);
      transform: translateY(-3px);
    }
    
    .cta-icon {
      width: 18px;
      height: 18px;
    }
    
    /* Stats */
    .hero-stats {
      display: flex;
      align-items: center;
      gap: 2rem;
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .stat-item {
      text-align: center;
    }
    
    .stat-number {
      display: block;
      font-size: 2rem;
      font-weight: 700;
      color: var(--primary-color);
      line-height: 1;
    }
    
    .stat-label {
      display: block;
      font-size: 0.875rem;
      color: var(--text-light);
      margin-top: 0.25rem;
    }
    
    .stat-divider {
      width: 1px;
      height: 40px;
      background: rgba(224, 225, 221, 0.3);
    }
    
    /* Floating Elements */
    .floating-elements {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 1;
    }
    
    .floating-card {
      position: absolute;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 16px;
      padding: 1rem;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      backdrop-filter: blur(10px);
      opacity: 0;
      animation: float 6s ease-in-out infinite;
    }
    
    .card-1 {
      top: 20%;
      left: 10%;
      animation-delay: 0s;
    }
    
    .card-2 {
      top: 60%;
      right: 15%;
      animation-delay: 2s;
    }
    
    .card-3 {
      top: 30%;
      right: 20%;
      animation-delay: 4s;
    }
    
    .card-content {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    
    .card-avatar {
      font-size: 2rem;
    }
    
    .card-info {
      display: flex;
      flex-direction: column;
    }
    
    .card-name {
      font-weight: 600;
      color: var(--text-primary);
      font-size: 0.875rem;
    }
    
    .card-rating {
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }
    
    .stars {
      font-size: 0.75rem;
    }
    
    .rating-text {
      font-size: 0.75rem;
      color: var(--text-primary);
      font-weight: 600;
    }
  </style>