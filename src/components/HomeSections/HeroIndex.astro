---
import Button from './Button.astro';
---

<section class="hero-section" id="home">
  <div class="hero-container">
    <div class="hero-content hero-animate">
      <span class="hero-badge">¡NUEVO!</span>
      <h1>Digitaliza el menú de tu restaurante <span class="highlight">en minutos</span></h1>
      <p>Con Menu-Upp, transforma tu carta tradicional en una experiencia digital interactiva que tus clientes amarán.</p>
      
      <div class="hero-features">
        <div class="feature">
          <span class="feature-icon">✓</span>
          <span>Fácil de personalizar</span>
        </div>
        <div class="feature">
          <span class="feature-icon">✓</span>
          <span>Sin comisiones ocultas</span>
        </div>
        <div class="feature">
          <span class="feature-icon">✓</span>
          <span>Integración sencilla</span>
        </div>
      </div>
      
      <div class="hero-buttons">
        <Button
          text="Ver demostración"
          href="#demo"
          variant="primary"
        />
        <Button
          text="Ver precios"
          href="#pricing"
          variant="outline"
        />
      </div>
      
      <div class="hero-note">
        <p>Lanzamiento especial: <strong>30% de descuento</strong> en todos los planes</p>
      </div>
    </div>
    
    <div class="hero-image-wrapper hero-animate">
      <div class="image-badge mode-badge">Menú Digital</div>
      <img 
        src="/images/menu-upp-hero.png" 
        alt="Menu-Upp Demo en dispositivos móviles" 
        class="hero-image"
        width="600"
        height="400"
        loading="eager"
        decoding="async"
      />
    </div>
  </div>
</section>

<style>
/* Estilos para la sección Hero */
.hero-section {
  position: relative;
  min-height: 90vh;
  padding: 6rem 2rem;
  overflow: hidden;
  background-color: var(--light);
  margin-top: 0;
}

.hero-container {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 2rem;
}

.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  right: 0;
  width: 45%;
  height: 100%;
  background-color: rgba(255, 69, 0, 0.05);
  clip-path: polygon(15% 0, 100% 0, 100% 100%, 0% 100%);
  z-index: 0;
}

.hero-content {
  position: relative;
  z-index: 1;
}

/* Clase especial para animación */
.hero-animate {
  opacity: 0;
  transform: translateY(20px);
  animation: heroFadeIn 0.5s ease forwards;
}

.hero-content.hero-animate {
  animation-delay: 0.2s;
}

.hero-image-wrapper.hero-animate {
  animation-delay: 0.4s;
}

@keyframes heroFadeIn {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-badge {
  display: inline-block;
  background-color: var(--primary);
  color: white;
  font-size: 0.85rem;
  font-weight: 600;
  padding: 0.3rem 0.8rem;
  border-radius: 20px;
  margin-bottom: var(--space-sm);
  letter-spacing: 0.5px;
}

.hero-section h1 {
  font-size: clamp(2rem, 4vw, 3.5rem);
  line-height: 1.2;
  margin-bottom: var(--space-md);
}

.highlight {
  color: var(--primary);
  position: relative;
  display: inline-block;
}

.highlight::after {
  content: '';
  position: absolute;
  bottom: 0.1em;
  left: 0;
  width: 100%;
  height: 0.2em;
  background-color: rgba(255, 69, 0, 0.2);
  z-index: -1;
}

.hero-section p {
  font-size: clamp(1rem, 1.5vw, 1.2rem);
  margin-bottom: var(--space-md);
  color: var(--gray);
  max-width: 540px;
}

.hero-features {
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
  margin-bottom: var(--space-md);
}

.feature {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.feature-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
  background-color: var(--primary);
  color: white;
  border-radius: 50%;
  font-size: 0.8rem;
}

.hero-buttons {
  display: flex;
  gap: var(--space-sm);
  margin-top: var(--space-md);
}

.hero-note {
  margin-top: var(--space-md);
  font-size: 0.9rem;
  padding: 0.6rem 1rem;
  background-color: rgba(255, 69, 0, 0.08);
  border-radius: var(--radius-sm);
  display: inline-block;
}

.hero-note p {
  margin: 0;
  font-size: 0.9rem;
}

.hero-image-wrapper {
  position: relative;
  z-index: 2;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  padding: 1rem;
  box-sizing: border-box;
}

.image-badge {
  position: absolute;
  background-color: white;
  padding: 0.4rem 1rem;
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
  font-weight: 600;
  /* box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); */
  z-index: 3;
  white-space: nowrap;
}

.mode-badge {
  top: 15%;
  left: 0;
  transform: translateX(-30%);
  color: var(--primary);
  background-color: white;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

.hero-image {
  width: 100%;
  max-width: 550px;
  height: auto;
  object-fit: contain;
  border-radius: 12px;
  /* box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1); */
  animation: heroFloat 6s ease-in-out infinite;
}

@keyframes heroFloat {
  0% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-15px);
  }
  100% {
    transform: translateY(0px);
  }
}

/* Media queries */
@media (max-width: 992px) {
  .hero-container {
    grid-template-columns: 55% 45%;
  }
}

@media (max-width: 768px) {
  .hero-section {
    min-height: auto;
    padding: 5rem 1.5rem 3rem;
    margin-top: 1rem;
  }
  
  .hero-container {
    grid-template-columns: 1fr;
    text-align: center;
  }
  
  .hero-section::before {
    width: 100%;
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 85%);
    height: 50%;
    opacity: 0.5;
  }
  
  .hero-content {
    order: 1;
  }
  
  .hero-features {
    max-width: 300px;
    margin-left: auto;
    margin-right: auto;
    align-items: flex-start;
  }
  
  .hero-buttons {
    justify-content: center;
  }
  
  .hero-image-wrapper {
    order: 0;
    margin-bottom: 2rem;
    width: 85%;
    margin-left: auto;
    margin-right: auto;
  }
  
  .mode-badge {
    top: 5%;
    left: 0;
    transform: translateX(-20%);
  }
  
  .hero-image {
    width: 100%;
    animation: none;
  }
}

@media (max-width: 576px) {
  .hero-section {
    padding: 4rem 1rem 2rem;
    margin-top: 2rem; /* Espacio adicional para separar del header en móvil */
  }
  
  .hero-section h1 {
    font-size: 1.75rem;
  }
  
  .hero-section p {
    font-size: 1rem;
  }
  
  .hero-features {
    align-items: center;
  }
  
  .hero-buttons {
    flex-direction: column;
    gap: 0.75rem;
    width: 100%;
    max-width: 300px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .hero-image {
    max-width: 95%;
  }
  
  .image-badge {
    font-size: 0.75rem;
    padding: 0.3rem 0.7rem;
  }
}
</style>

<script>
  // Script para manejar las animaciones al cargar la página
  document.addEventListener('DOMContentLoaded', () => {
    // Ya no es necesario agregar clases 'appear' porque usamos animaciones CSS directas
    // Esto evita conflictos con los estilos globales
  });
</script>